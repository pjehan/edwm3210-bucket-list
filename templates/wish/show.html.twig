{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {{ wish.title }}{% endblock %}

{% block body %}
    <h1>{{ wish.title }}</h1>
    <p>Ajouté le {{ wish.createdAt|date('d/m/Y') }}</p>
    {% include 'wish/_metadata.html.twig' with {'wish': wish} only %}
    <p>{{ wish.description }}</p>
{% endblock %}

